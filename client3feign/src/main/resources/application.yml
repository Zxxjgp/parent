server:
  port: 1113


feign:
  client:
    config:
      default:
        connect-timeout: 5000
        read-timeout: 500
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, health, info
  endpoint:
    health:
      show-details: always

